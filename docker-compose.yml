services:
  app:
    build:
      context: .
      dockerfile: app/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - TZ=Asia/Seoul
    volumes:
      - ./models:/workspace/models
      - ./app/data/:/workspace/app/data/
